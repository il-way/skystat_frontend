# AdSense 설정 가이드

이 문서는 SkyStat 공개 페이지의 AdSense 승인 전/후 작업을 빠르게 수행하기 위한 운영 문서입니다.

## 1) 승인 전 체크리스트

### 정책/고지 페이지 링크 확인
- [ ] 상단 네비/푸터에서 `Privacy` 페이지로 이동 가능
- [ ] 상단 네비/푸터에서 `Terms` 페이지로 이동 가능
- [ ] 상단 네비/푸터에서 `Contact` 페이지로 이동 가능
- [ ] 상단 네비/푸터에서 `About` 페이지로 이동 가능

### 콘텐츠 품질 확인
- [ ] `Guide` 페이지에 지표 해석/임계값/코드 참고 등 설명성 콘텐츠가 충분함
- [ ] `FAQ` 페이지에 검색/카테고리/질문-답변(정책/기술 포함)이 구성됨
- [ ] `Report` 페이지가 실제 지표(KPI/차트/월별 관측 일수)를 명확히 보여줌
- [ ] 페이지 텍스트가 너무 짧지 않고, 사용자 의도를 충족할 수준의 정보량을 가짐

### 광고 배치 원칙(사전 설계)
- [ ] 버튼, 정렬 토글, 입력폼, 검색창 근처에 광고를 붙이지 않음
- [ ] 오인 클릭 유도(콘텐츠처럼 위장, 클릭 유도 문구) 금지
- [ ] 주요 조작 UI와 광고 사이에 충분한 간격 확보
- [ ] 모바일에서도 광고와 UI가 겹치지 않도록 레이아웃 검토

## 2) 승인 후 필수 작업

### ads.txt 업데이트
- 현재 `public/ads.txt`는 플레이스홀더 상태
- [ ] AdSense 계정에서 제공한 공식 라인으로 교체
- [ ] 배포 후 `https://<domain>/ads.txt` 접근 확인

### CMP 도입(EEA/UK/CH 대상)
- [ ] Google 인증 CMP 사용 검토/도입
- [ ] 동의 모드(Consent Mode) 적용
- [ ] 사용자 동의 상태에 따라 광고 처리 분기
  - 개인화 광고(Personalized Ads)
  - 비개인화 광고(Non-personalized Ads)

### 광고 슬롯 배치 권장 위치
- Home: [ ] 섹션 사이 1개
- Guide: [ ] 하단 1개
- FAQ: [ ] 하단 1개
- Report: [ ] 테이블 아래 1개
  - 주의: 버튼/토글/정렬 UI 근처 배치 금지
- Dashboard: [ ] 초기 단계에서는 광고 배치 보류(UX 민감 영역)

## 3) 트러블슈팅

### Clipboard 제한
- `navigator.clipboard`는 보안 컨텍스트(HTTPS)에서 동작이 안정적
- localhost/HTTP 환경에서는 동작 제한이 발생할 수 있음

### SPA 직접 접속 이슈
- `/report/...`, `/guide` 등 직접 URL 접근 시 서버가 `index.html` fallback을 반환해야 함
- 라우터 404가 발생하면 웹서버 리라이트 설정 점검

### CLS(광고 로딩으로 인한 레이아웃 흔들림) 방지
- 광고 슬롯 컨테이너에 `min-height`를 미리 지정
- 초기 렌더 시 자리 확보 후 광고 로드
- 모바일/데스크톱 각각 슬롯 높이 검증

---

운영 메모:
- 정책 문구(Privacy/Terms)는 광고 운영 방식 변경 시 즉시 동기화합니다.
- 배치/수익보다 정책 준수와 UX 안정성을 우선합니다.
